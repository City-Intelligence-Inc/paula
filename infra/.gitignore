# Terraform state and provider binaries
.terraform/
*.tfstate
*.tfstate.backup

# Secret variable values — NEVER commit
terraform.tfvars

# Provider lock file (regenerated by terraform init)
.terraform.lock.hcl
